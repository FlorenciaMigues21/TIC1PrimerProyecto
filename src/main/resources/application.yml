 spring:
  application:
    name: server
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: admin
  jpa:
    show-sql: true
    hibernate.ddl-auto: update
    generate-ddl: true
  logging:
    level:
      ROOT: INFO